#include<bits/stdc++.h>
using namespace std;
using ll = long long;
int MOD = 1e9 + 7;

int main(){
    int n; cin >> n;
    int a[n];
    for(int &x : a) cin >> x;
    stack <int> st;
    vector <int> tmp;
    reverse(a, a + n);
    for(int i = 0; i < n; i++){
        
    }
    return 0;
}
